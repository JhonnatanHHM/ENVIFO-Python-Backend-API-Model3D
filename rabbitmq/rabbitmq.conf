# Límite de memoria: 40% de la memoria asignada al contenedor (300 MB → ~120 MB)
vm_memory_high_watermark.relative = 0.4

# Límite de disco: no dejar que baje de 50 MB libres
disk_free_limit.absolute = 50MB

# Evita crash loop si hay presión de recursos
cluster_partition_handling = pause_minority
