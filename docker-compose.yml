version: "3.9"

services:
  python-api:
    build: ./python-api
    container_name: python-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - rabbitmq
      - blender
    volumes:
      - shared_data:/app/data
    deploy:
      resources:
        limits:
          memory: 300m
          cpus: "0.5"

  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS}
      RABBITMQ_VM_MEMORY_HIGH_WATERMARK: 0.4   # 40% de la RAM asignada → evita que explote
      RABBITMQ_DISK_FREE_LIMIT: 50MB           # asegura que no consuma disco infinito
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    deploy:
      resources:
        limits:
          memory: 300m
          cpus: "0.5"

  blender:
    build: ./blender-service
    container_name: blender
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - shared_data:/app/data
    deploy:
      resources:
        limits:
          memory: 350m
          cpus: "0.5"

volumes:
  rabbitmq_data:
  shared_data:
